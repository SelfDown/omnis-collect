service:
  # 项目
  - key: "create_jms_interface"
    module: 'monitor'# 执行监控 查询
    must_login: false
    params:
      new_jms_interface_list:
        check:
          template: "{{new_jms_interface_list|must_list}}"
          err_msg: "接口列表不能为空"
    data_json: create_jms_interface.json
    log: true
