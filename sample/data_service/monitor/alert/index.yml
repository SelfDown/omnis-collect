service:
  # 项目
  - key: "alert"
    module: 'monitor'# 执行监控 查询
    must_login: false
    data_json: alert.json
    result_handler:
      - key: new_col
        params:
          to_field:
            - field: 'time'
              template: "{{clock|clock_time}}"
    log: true