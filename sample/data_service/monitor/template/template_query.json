{

  "method": "template.get",
    "params": {
        "output": "extend",
        "filter": {
            "host": [
              {% if is_windows =='True' %}
                "Windows OS Template"
              {% else %}
                "Linux OS Template"
              {% endif %}

            ]
        }
    },
  require('../common/session.common')
}