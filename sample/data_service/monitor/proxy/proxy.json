{

    "method": "proxy.get",
    "params": {
        "output": "extend",
        "selectInterface": "extend",
        "search": {
            "host": "{{monitor_proxy_name}}"
        }
    },
   require('../common/session.common')
}