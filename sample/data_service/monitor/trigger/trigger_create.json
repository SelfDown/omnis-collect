{

  "method": "trigger.create",
  "params":  {

      "description":  "[CKE003]-{{server_name}}：{{soft_server.server_ip}}-{{detail_info}}",
      "priority": 4,
      "expression": "{{'{'+monitor_server.host}}:{{soft_server.key}}.str(NOT_OK)}=1"
  },
   require('../common/session.common')
}