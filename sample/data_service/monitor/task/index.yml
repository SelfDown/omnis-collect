service:
  # 项目
  - key: "check_now"
    module: 'monitor'# 执行监控 查询
    must_login: false
    params:
      item_id_list:
        check:
          template: "{{item_id_list|must_list}}"
          err_msg: "监控列表不能为空"
        name: 监控列表
    data_json: check_now.json
    log: true